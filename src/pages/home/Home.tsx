import React from 'react';
import { BaseTitle } from '../../components/shared/styledComponents';

const Home = () => {
  return (
    <div>
      <BaseTitle>Welcome to Homepage</BaseTitle>
    </div>
  );
};

export default Home;
