import React from 'react';
import { BaseTitle } from '../../components/shared/styledComponents';

const About = () => {
  return (
    <div>
      <BaseTitle> About page</BaseTitle>
    </div>
  );
};

export default About;
